{
  "name": "@app/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./prisma.ts",
  "types": "./prisma.ts",
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "studio": "prisma studio",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings=0",
    "test": "echo \"no tests in @app/database yet\" && exit 0"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "prisma": "^5.20.0",
    "typescript": "^5.6.3"
  }
}
